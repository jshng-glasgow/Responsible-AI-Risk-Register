name: Issue to Resource CSV

on:
  issues:
    types: [labeled]

concurrency:
  group: resource-update
  cancel-in-progress: false

jobs:
  add-resource-direct:
    if: github.event.label.name == 'new resource' && vars.WORKSHOP_AUTO_PUBLISH_NEW_RISKS == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.repository.default_branch }}
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Synchronise with latest default branch
        run: git pull --ff-only origin "${{ github.event.repository.default_branch }}"

      - name: Parse issue and update resources
        env:
          ISSUE_BODY: ${{ github.event.issue.body }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
        run: python .github/scripts/resource_to_csv.py

      - name: Install render dependencies
        run: pip install pandas

      - name: Validate and regenerate site data
        run: |
          python .github/scripts/validate_resources.py
          python .github/scripts/render_table.py

      - name: Commit directly to default branch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add resources/resources.csv docs/index.html docs/resources.json
          if git diff --cached --quiet; then
            echo "No resource changes detected"
            exit 0
          fi
          git commit -m "Add resource from issue #${{ github.event.issue.number }}"
          git push origin HEAD:${{ github.event.repository.default_branch }}

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./docs

  add-resource-pr:
    if: github.event.label.name == 'new resource' && vars.WORKSHOP_AUTO_PUBLISH_NEW_RISKS != 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Parse issue and update resources
        env:
          ISSUE_BODY: ${{ github.event.issue.body }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
        run: python .github/scripts/resource_to_csv.py

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v6
        with:
          commit-message: "Add resource from issue #${{ github.event.issue.number }}"
          title: "Add resource: ${{ github.event.issue.title }}"
          body: "Automatically generated from issue #${{ github.event.issue.number }}"
          branch: "resource/issue-${{ github.event.issue.number }}"
          labels: "auto-generated"
