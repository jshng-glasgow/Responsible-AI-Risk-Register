name: Render HTML Table

on:
  push:
    paths:
      - 'register/risks.csv'
      - 'resources/resources.csv'
      - '.github/scripts/render_table.py'
      - 'docs/app.js'
      - 'docs/styles.css'

jobs:
  render:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: pip install pandas

      - name: Validate data
        run: |
          python .github/scripts/validate_csv.py
          python .github/scripts/validate_resources.py

      - name: Generate HTML
        run: python .github/scripts/render_table.py

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./docs
